{
  "name": "vietacademia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A searchable directory of Vietnamese professors in Computer Science at U.S. universities.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate-data": "node scripts/validate-data.js",
    "test": "npm run validate-data && node --test test/data.test.js test/ui.test.js",
    "test:e2e": "node --test test/browser-smoke.test.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vite": "^7.2.4"
  }
}
