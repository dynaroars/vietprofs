{
  "name": "vietacademia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A directory of Vietnamese professors worldwide.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate-data": "node scripts/validate-data.js",
    "test": "npm run validate-data && node --test test/data.test.js test/maintenance-controller.test.js test/ui.test.js",
    "test:e2e": "node --test test/browser-smoke.test.js"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "playwright": "^1.62.1",
    "vite": "^8.2.2"
  }
}
